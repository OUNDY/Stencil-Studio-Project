/**
 * Backward-compat re-export.
 * All content has moved to registry-config.ts — import from there directly.
 */
export { registry, getComponentRegistry } from "./registry-config";
export type { ComponentEntry, ComponentPlugin, ComponentCategory } from "./registry-config";
